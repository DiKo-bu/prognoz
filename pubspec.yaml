name: prognoz
description: Monte Carlo & Gantt Simulator
publish_to: 'none'
version: 1.0.7+10
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.19.0
  mqtt_client: ^10.0.0
  http: ^1.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
